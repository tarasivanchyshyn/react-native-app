import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    width: '100%',
    marginBottom: 12,
    padding: 10,
    borderColor: 'white',
    borderWidth: 2,
    borderRadius: 4,
    color: 'white',
    fontSize: 16
  }
});

export default styles;
